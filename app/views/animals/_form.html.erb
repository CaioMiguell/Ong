<%= form_for @animal, :html => { :class => "form-horizontal animal" } do |f| %>

  <% if @animal.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@animal.errors.count, "error") %> prohibited this animal from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @animal.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>
jjjjjjjjjjjjjjjjjjjjjjjjj
  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>
  <div class="form-group">
    <%= f.label :image, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.file_field :image, :class => 'form-control' %>
    </div>
    <%=f.error_span(:image) %>
  </div>
  <div class="form-group">
    <%= f.label :species, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :species, :class => 'form-control' %>
    </div>
    <%=f.error_span(:species) %>
  </div>
  <div class="form-group">
    <%= f.label :breed, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :breed, :class => 'form-control' %>
    </div>
    <%=f.error_span(:breed) %>
  </div>
  <div class="form-group">
    <%= f.label :note, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :note, :class => 'form-control' %>
    </div>
    <%=f.error_span(:note) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                animals_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
<%= render partial: 'form', formats: [:js], locals: {time_now: Time.now} %>
